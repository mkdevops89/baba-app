apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Additional Prometheus resources managed through GitOps.
# The Helm chart deploys the monitoring platform, while these manifests
# define Baba App-specific monitoring integrations.
resources:
  - baba-app-servicemonitor.yaml
  - baba-app-dashboard.yaml